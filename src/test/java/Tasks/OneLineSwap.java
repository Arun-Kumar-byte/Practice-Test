package Tasks;

public class OneLineSwap {

	//Interview-Preferred One-Line Swap (Using Arrays)

	/* int[] arr = {10, 20};
	arr[0] = arr[0] + arr[1] - (arr[1] = arr[0]);


	//One-Line Swap Using Java Features (Objects)
	Integer a = 10, b = 20;
	a = a + b - (b = a);
*/

}
